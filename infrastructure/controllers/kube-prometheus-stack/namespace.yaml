# `privileged` because node-exporter mounts the host root and runs with
# hostNetwork and hostPID, which Talos's default `baseline` rejects.
apiVersion: v1
kind: Namespace
metadata:
  name: monitoring
  labels:
    pod-security.kubernetes.io/enforce: privileged
