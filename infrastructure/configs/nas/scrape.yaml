# node-exporter as a TrueNAS custom app -- see machines/truenas.md.
apiVersion: monitoring.coreos.com/v1alpha1
kind: ScrapeConfig
metadata:
  name: nas
  namespace: monitoring
spec:
  jobName: nas
  staticConfigs:
    - targets:
        - nas-k8s.${lab_domain}:9100
      labels:
        node: nas
